# Phase 6: causal test of the land-allocation hypothesis — baseline representativeness check FAILED

Per the assigned methodology's step 1 (the explicit fix for Phase 4B's own
documented mistake), before building any intervention variant this phase
first verified that a candidate baseline's own BUY_LAND reach-rate actually
matches Phase 5's measured real-play figure (59.3%, 89/150 games). It does
not, for either plausible current-champion candidate, so no intervention
variant was built. See `REPORT_PHASE6_LAND_ALLOCATION.md` for the full
writeup; `verify_baseline.py` is the check script.
