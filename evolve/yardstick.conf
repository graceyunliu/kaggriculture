# Sourced by evolve/supervisor.sh at the start of every segment (after git pull).
# Yardstick = H32 (Sep 5, replaces H10 — H10 predates H31/H32/M2 and was stale as of Sep 5).
# Calibration: C1 scores -37,097/game vs H32 on seeds 1-10 both seats (t=-9.33, 0-10) —
# roughly 2.6x worse than C1's -14k vs the old H10 yardstick, confirming H32 is a
# meaningfully stronger frontier. SMOKE_FLOOR/DEV_PROMOTE below are PROVISIONAL:
# P6 (candidates/P6_baseline.py, this project's best own-code planner and the real
# "competitive but not winning" reference) was not available on this checkout to measure
# directly — see tools/calibrate_yardstick.py. Estimated from P6's documented -$9,374/game
# vs H10 and the general -$23k-$27k/game frontier-tape gap, scaled by the same ~2.6x factor
# C1 showed. Re-run tools/calibrate_yardstick.py once P6_baseline.py is committed and
# replace these with the measured value, not this estimate.
FRONTIER=candidates/H32.py
SMOKE_FLOOR=-40000
DEV_PROMOTE=-20000
